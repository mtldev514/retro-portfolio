# Cloudinary Configuration
# Sign up at https://cloudinary.com (free tier available)
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# GitHub Configuration (Optional - for large media files like music)
# Create a Personal Access Token at https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=your_github_personal_access_token_here

# Notes:
# - Copy this file to .env and fill in your actual values
# - Never commit .env to git (it's in .gitignore)
# - Cloudinary is used for images (required)
# - GitHub is used for music/video files (optional, falls back to Cloudinary)
# - GitHub repo and release tag are configured in config/app.json
